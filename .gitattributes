# Dateien mit Integrity-Pruefung (SRI) duerfen NICHT zeilenende-konvertiert
# werden. core.autocrlf=true machte aus 33 LF in vis-network.min.js 33 CRLF —
# 33 Byte mehr, anderer sha384, und der Browser BLOCKIERT das Skript. Live lief
# es (Cloudflare klont mit LF), lokal war der Code-Graph deshalb IMMER leer:
# kein `vis`, kein `network`, kein Fehler im Bild.
Assets/js/vendor/** -text
