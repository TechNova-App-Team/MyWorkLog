# Zusaetzlich zu .gitignore. Syntax wie .gitignore.
#
# Minifizierte Fremd-Bibliotheken: Der AST-Parser findet dort nur die
# Ein-Buchstaben-Funktionen des Minifiers (a(), b(), k() ...). Die landen
# als Knoten im Graphen, gehoeren zu keinem sinnvollen Zusammenhang und
# tauchen dann in Abfrageergebnissen auf, in die sie nicht gehoeren.
*.min.js
*.min.css

# Generierte Artefakte (stehen so auch in .gitignore, hier nochmal explizit,
# damit ein lokaler Build den Graphen nicht mit Doppeleintraegen aufblaeht).
/index.html
/pages/en/
