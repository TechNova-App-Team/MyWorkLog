name = "analytics-proxy"
main = "src/index.js"
compatibility_date = "2025-01-01"
account_id = "ab30e1403c63aa439e15acfd8ee8a080"

# Secret wird via `wrangler secret put CF_API_TOKEN` gesetzt:
#   - Account → Account Analytics: Read
#   - Zone    → Zone Analytics: Read (myworklog.de)

# Custom Domain bindest du im CF Dashboard:
#   Workers & Pages → analytics-proxy → Settings → Triggers → Custom Domains
#   → analytics-proxy.myworklog.de
